
import { oldHour } from 'tennis-manager/helpers/old-hour';
import { module, test } from 'qunit';

module('Unit | Helper | old hour');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oldHour([42]);
  assert.ok(result);
});

